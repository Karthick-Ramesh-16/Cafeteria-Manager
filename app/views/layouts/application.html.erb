<!DOCTYPE html>
<html>
  <head>
    <title>CafeteriaManager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: "all", 'data-turbolinks-track': "reload" %>
    <%= javascript_pack_tag "application", 'data-turbolinks-track': "reload" %>
  </head>

  <body>
    <% if @current_user.present? %>
      <ul class="UserHeader">
        <li><%= link_to "Home", menu_categories_path, class: "UserHeader-home" %></li>
        <li><%= link_to "Orders", orders_path, class: "UserHeader-order" %></li>
        <li><%= link_to "Cart", cart_items_path, class: "UserHeader-cart" %></li>
        <li><%= link_to "Sign out", destroy_session_path, method: "DELETE", class: "UserHeader-sign-out" %></li>
        <li class="UserHeader-name"><%= @current_user.first_name + " " + @current_user.last_name %></li>
      </ul>
    <% end %>

    <% if flash[:error] %>
      <p class="Flash-error"><%= flash[:error] %></p>
    <% end %>

    <%= yield %>
  </body>
</html>
